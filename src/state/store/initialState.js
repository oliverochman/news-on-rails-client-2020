const initialState = {
  currentUser: { email: undefined, role: undefined },
  authenticated: false,
  renderLoginForm: false,
  errorMessage: "",
  location: undefined,
};

export default initialState;